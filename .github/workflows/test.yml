name: CI Test
on:
  push:
    branches: [dev]
  pull_request:
    branches: [main]

jobs:
  syntax-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: JS syntax check
        run: |
          echo "=== Перевірка JS файлів ==="
          FAILED=0
          for f in *.js; do
            node --check "$f" 2>&1 && echo "✅ OK: $f" || { echo "❌ FAIL: $f"; FAILED=1; }
          done
          exit $FAILED

      - name: HTML check
        run: |
          python3 -c "
          with open('index.html', encoding='utf-8') as f:
              c = f.read()
          assert '</html>' in c
          print('✅ HTML OK')
          "